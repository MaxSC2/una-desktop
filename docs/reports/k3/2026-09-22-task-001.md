# AGENT REPORT: TASK-001 — приёмочные тесты `electron/ai/compression.ts`

- **Agent:** K3-зона (выполнено оркестратором по прямому мандату владельца 2026-09-22)
- **Branch:** `k3/task-001-compression-tests`
- **Status:** DONE

## 1. Findings (Что обнаружено)

1. Контракт TASK-001 полностью соответствует реальному поведению `compression.ts` —
   расхождений «код vs контракт» не выявлено, правки продакшн-кода не потребовались (P1-2 соблюдён).
2. **CI PR #1 падал не из-за кода проекта:** `npm ci` → postinstall
   `electron-builder install-app-deps` → исходная сборка `better-sqlite3` →
   `prebuild-install` не нашёл пребилд → `node-gyp 9.4.1` не распознаёт
   Visual Studio 18 на текущем образе `windows-latest`
   (`unknown version "undefined" at C:\Program Files\Microsoft Visual Studio\18\Enterprise`).
   Падали обе джобы-родителя (Lint, Type Check) на шаге Install dependencies; Build — skipped.
3. **Побочная находка (исправлена отдельным коммитом):** `sync-manifest.js` в git-worktree
   добавлял служебный gitfile `.git` в манифест (EXCLUDE_DIRS ловит только каталог `.git/`).
4. **Предсуществующее расхождение манифеста (не исправлено, вне scope):** манифест на
   основном дереве (329) включает 39 untracked-файлов `docs/design/prototype-scene-vNext/**`,
   которых нет в git → в любом чистом checkout/worktree verify дал бы «39 пропущено».
   В worktree-манифесте (291) их нет; при прогоне sync на основном дереве они вернутся.
   Связано с pending-задачей №6 (`.review-profile/` в EXCLUDE_DIRS) — семантика манифеста
   (disk-state vs git-state) требует решения владельца.

## 2. Changes (Что изменено)

| Коммит | Тема | Файлы |
|---|---|---|
| `596ae18` | ci: `npm ci --ignore-scripts` на раннерах | `.github/workflows/ci.yml` |
| `f16f032` | fix(manifest): исключить `.git`-gitfile | `scripts/sync-manifest.js`, `scripts/verify-manifest.js` |
| `753b1aa` | test(k3): TASK-001 приёмочные тесты + manifest sync | `tests/ai/compression.test.ts` (новый, 26 кейсов), `scripts/manifest-files.json`, `MANIFEST.md` |

Продакшн-код (`electron/**`, `src/**`) не изменён ни в одном коммите.

Покрытие контракта (26 кейсов):
- `promoteFrequentFacts`: порог `use_count >= 5` → `preference` с сохранением `use_count`;
  `< 5` и чужие категории не трогаются; лимит 10; пустая БД → 0; `getDb()=null` → 0. (6)
- `demoteStaleFacts`: stale → `markFactForget` (use_count=-1, без DELETE); NULL-поля;
  защита `project`/`preference`; свежие и `use_count >= 3` не трогаются; лимит 20; `getDb()=null`. (7)
- `compressOldConversations`: фильтр «старше 7 дней без summary»; гейт `shouldMaintainMemory()`;
  пустой LLM-ответ (сообщения целы, summary не пишется); успешная запись summary;
  эпизодический fallback при `stored:false` и при исключении `remember()`; сэмпл 3+3 с
  дедупликацией по id; срез 800 символов; лимит 5 бесед; беседа без сообщений; `getDb()=null`. (11)
- `runMaintenance`: агрегат `{compressed, promoted, demoted}` + нулевой агрегат. (2)

## 3. Evidence (Тесты, сборка, хэши)

Среда: worktree `../UNA-k3-workzone`, node_modules зеркалирован (без `npm install`, npm bug #4828).

| Проверка | Результат |
|---|---|
| `npx vitest run tests/ai/compression.test.ts` | ✅ 26/26 (exit 0) |
| `npx vitest run` (весь набор) | ✅ 276/276, 14 файлов (250 → 276) |
| `npx tsc -p electron/tsconfig.json --noEmit` | ✅ 0 ошибок |
| `npx tsc --noEmit` (renderer/root) | ✅ 0 ошибок |
| `node scripts/verify-manifest.js` | ✅ 291/291, 0 пропущено / 0 лишних |
| GitHub Actions CI (PR #1, run 35763907470) | ✅ Lint / Type Check / Build — success |

Хэши: `596ae18` (ci) → `f16f032` (manifest fix) → `753b1aa` (tests) на `k3/task-001-compression-tests`.

## 4. Architecture Impact (Влияние на архитектуру и DEC)

- Инварианты не нарушены: новых подсистем/роутеров/моделей нет; tool-loop, safety, router не тронуты.
- DEC-021: первая задача K3-зоны выполнена; веточная изоляция и push только в `k3/*` соблюдены.
- DEC-012: монотематические коммиты (ci / manifest-fix / tests / docs).
- DEC-011 (конфликт, требует решения): ruleset на `main` (PR + approving review) делает
  прежний поток «коммит напрямую в main» невозможным для всех, включая владельца PAT.
  Зафиксировано как открытое процессное решение (см. §5), DEC-011 не переписывался.

## 5. Decisions Required (Требуют решения владельца)

1. **PR #1 merge:** ruleset требует approving review от reviewer с write access;
   единственный такой аккаунт — автор PR (self-approval невозможен). Нужно действие
   владельца в GitHub UI: Approve → Merge. Либо пересмотреть ruleset (решение за человеком).
2. **DEC-011 vs PR-based workflow:** выбрать — bypass-actor для владельца, или полный
   PR-flow для всех агентов (включая K3: merge k3-веток в main через PR с чьим review?).
3. **Семантика манифеста:** disk-state (с untracked design-файлами) vs git-state —
   решить перед стартом pending-задачи №6 (`.review-profile/`).

## 6. Risks & Next Actions (Риски и следующий шаг)

- **Риск:** манифест ветки k3 (291) расходится с основным деревом (329, untracked
  design-файлы) — при sync на основном дереве design-записи вернутся; это ожидаемо.
- **Риск:** `npm ci --ignore-scripts` на CI означает, что нативная сборка better-sqlite3
  в CI не проверяется. Если появится CI-джоба, исполняющая нативные модули (тесты,
  packaging), понадобится пребилд-стратегия или рабочий node-gyp toolchain на раннере.
- **Следующая K3-задача (рекомендация):** TASK-002 — приёмочные тесты
  `electron/ai/resource-manager.ts` (тот же контракт P1-2, тот же шаблон моков; закрывает
  следующий пункт пробела №5 implementation-map). Спецификацию готовить по образцу TASK-001.

